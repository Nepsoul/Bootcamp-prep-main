let counter = 0;

// YOUR CODE BELOW

function incrementalChange(number) {
  counter = counter + number;
  //currentValue = counter + number;  //need current counter value as well as initial number.
  return counter;
}
//incrementalChange(2);
//console.log(incrementalChange(2));
//console.log(incrementalChange(4));
