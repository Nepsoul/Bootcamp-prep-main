// YOUR CODE BELOW
function whosASpecial(array) {
  let result = [];

  array.forEach(arr);
  function arr(petobject) {
    result.push(`${petobject.name} the ${petobject.species} is very special!`);
  }
  return result.join(" ");
}
